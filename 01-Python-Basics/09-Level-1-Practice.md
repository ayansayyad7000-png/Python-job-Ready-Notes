# 09 — Level 1 Practice

Solve these without copying solutions.

## Part A — Basics
1. Print your name and role.
2. Create variables for name, age, salary, active status, and skills.
3. Print the type of each variable.
4. Use an f-string to print a profile summary.

## Part B — Input and Conversion
5. Ask for birth year and current year; calculate age.
6. Ask for monthly salary; calculate annual salary.
7. Ask for product price and quantity; calculate total bill.

## Part C — Operators
8. Print the remainder of a number divided by 2.
9. Check whether age is at least 18 and ID is available.
10. Check whether `"Python"` exists in a skills list.

## Predict the Output
```python
x = "10"
y = "20"
print(x + y)
```

```python
print(bool(0))
print(bool("0"))
```

## Mini Project — Employee Salary Summary
Input:
- employee ID
- employee name
- monthly salary
- bonus percentage

Calculate:
```text
annual_salary = monthly_salary * 12
bonus = annual_salary * bonus_percentage / 100
final_compensation = annual_salary + bonus
```

Print a clean summary using f-strings.

## Self-Check
You should now be comfortable with variables, types, input/output, operators, conversion, and basic formatting.

## Interview Rapid Fire
1. What does `input()` return?
2. `=` vs `==`?
3. `/` vs `//`?
4. `is` vs `==`?
5. What does `None` represent?