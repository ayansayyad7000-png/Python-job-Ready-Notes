# 04 — Functions Practice

Build these without copying:

1. `add(a, b)`
2. `is_even(n)` returns bool
3. `calculate_salary(monthly, bonus_percent=0)`
4. `normalize_email(email)` using strip/lower
5. `find_max(numbers)` without built-in `max`
6. `average(*numbers)`
7. `build_config(**kwargs)` returns dict
8. Sort employees by salary using lambda

## Company Task — Retry Config
Create:
```python
def create_retry_config(max_attempts=3, timeout=30, **extra):
    ...
```
Return one dictionary with all settings.

## Self Check
- [ ] def
- [ ] parameters/arguments
- [ ] return
- [ ] defaults
- [ ] keyword args
- [ ] *args
- [ ] **kwargs
- [ ] lambda
- [ ] scope
